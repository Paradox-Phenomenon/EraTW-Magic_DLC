;Double Handjob
@ADD_CUSTOM_SCOM12
CALL ADD_CUSTOM_SCOM_MODULE12
CALL ADD_SEX_MODULE_EXECUTE

RETURN 1

@ADD_CUSTOM_SCOM_ABLE12
#DIM DYNAMIC SUCCEED
CALL ADD_CUSTOM_SCOM_MODULE12
CALL ADD_SEX_MODULE_COM_ABLE
SIF !RESULT
	RETURN 0

;everyone needs moderate finger skill
FOR LOCAL, 0, 2
	SIF ABL:(ADD_SEX_MODULE_CHARA:LOCAL):指 < 2
		RETURN 0
NEXT

RETURN 1

@ADD_CUSTOM_CAN_SCOM12(ARG)
SKIPDISP ARG
PRINTFORML Double Handjob
SKIPDISP 0

CALL ADD_CUSTOM_SCOM_MODULE12
CALL ADD_SEX_MODULE_CAN_COM(ARG)
RETURN RESULT

@ADD_CUSTOM_MESSAGE_SCOM12
#DIM CURRENT_CHARA
#DIM OTHER_CHARA
#DIM SKILL_DIFFERENCE

SKILL_DIFFERENCE = (ABL:(ADD_SEX_MODULE_CHARA:0):指 * 3 + ABL:(ADD_SEX_MODULE_CHARA:0):技巧) - (ABL:(ADD_SEX_MODULE_CHARA:1):指 * 3 + ABL:(ADD_SEX_MODULE_CHARA:1):技巧)

IF NOWEX:MASTER:射精
	PRINTFORMDL As you near ejaculation, %CALLNAME:(ADD_SEX_MODULE_CHARA:0)% and %CALLNAME:(ADD_SEX_MODULE_CHARA:1)% speed up their movements, pushing you over the edge.
ELSE
	IF TALENT:PLAYER:双槍
		IF ADD_PREV_SELECTED_SCOM == 12
			PRINTFORMDL %CALLNAME:(ADD_SEX_MODULE_CHARA:0)% and %CALLNAME:(ADD_SEX_MODULE_CHARA:1)% continue to stroke your %TINKO_TR(PLAYER, 1)% together.
		ELSE
			PRINTFORMDL %BREAKENG(@"While %CALLNAME:(ADD_SEX_MODULE_CHARA:1)% continues to stroke one of your %TINKO_TR(PLAYER, 1)%, %CALLNAME:(ADD_SEX_MODULE_CHARA:0)% grabs the other and begins stroking it as well.")%
		ENDIF

		;both are skilled
		IF ABL:(ADD_SEX_MODULE_CHARA:0):技巧 > 5 && ABL:(ADD_SEX_MODULE_CHARA:0):指 > 3 && ABL:(ADD_SEX_MODULE_CHARA:1):技巧 > 5 && ABL:(ADD_SEX_MODULE_CHARA:1):指 > 3
			PRINTFORMDL Expertly matching their pace, their caresses send immense waves of pleasure through your %PLURALIZER(TINKO_TR(PLAYER))%.
		;one is more skilled
		ELSEIF SKILL_DIFFERENCE
			PRINTFORMDL %CALLNAME:(ADD_SEX_MODULE_CHARA:(SKILL_DIFFERENCE > 0 ? 1 # 0))% has difficulty keeping up with the more skilled %CALLNAME:(ADD_SEX_MODULE_CHARA:(SKILL_DIFFERENCE > 0 ? 0 # 1))%, leading to an uneven feeling in your %PLURALIZER(TINKO_TR(PLAYER))%.
		;equally unskilled
		ELSE
			PRINTFORMDL Neither of them is particularly skilled, and it's clear that they have a hard time working together.
		ENDIF
	ELSE
		IF ADD_PREV_SELECTED_SCOM == 12
			PRINTFORMDL %CALLNAME:(ADD_SEX_MODULE_CHARA:0)% and %CALLNAME:(ADD_SEX_MODULE_CHARA:1)% continue to stroke your %TINKO_TR(PLAYER, 1)% together.
		ELSE
			PRINTFORMDL %BREAKENG(@"%CALLNAME:(ADD_SEX_MODULE_CHARA:0)% joins %CALLNAME:(ADD_SEX_MODULE_CHARA:1)% and begins stroking your %TINKO_TR(PLAYER, 1)% as well.")%
		ENDIF

		;both are skilled
		IF ABL:(ADD_SEX_MODULE_CHARA:0):技巧 > 5 && ABL:(ADD_SEX_MODULE_CHARA:0):指 > 3 && ABL:(ADD_SEX_MODULE_CHARA:1):技巧 > 5 && ABL:(ADD_SEX_MODULE_CHARA:1):指 > 3
			PRINTFORMDL Their skill is obvious, and they eagerly work together to bring your %TINKO_TR(PLAYER)% as much pleasure as they can.
		;one is more skilled
		ELSEIF SKILL_DIFFERENCE
			PRINTFORMDL %CALLNAME:(ADD_SEX_MODULE_CHARA:(SKILL_DIFFERENCE > 0 ? 1 # 0))% is not as skilled as %CALLNAME:(ADD_SEX_MODULE_CHARA:(SKILL_DIFFERENCE > 0 ? 0 # 1))%, and keeps bumping into %HIS_HER(ADD_SEX_MODULE_CHARA:(SKILL_DIFFERENCE > 0 ? 0 # 1))%.
		;equally unskilled
		ELSE
			PRINTFORMDL Neither of them is particularly skilled, and they constantly bump into each other.
		ENDIF
	ENDIF

	IF ABL:(ADD_SEX_MODULE_CHARA:0):指 > 4 && ABL:(ADD_SEX_MODULE_CHARA:0):技巧 > 7 && ABL:(ADD_SEX_MODULE_CHARA:1):指 && ABL:(ADD_SEX_MODULE_CHARA:1):技巧 > 7 && !RAND:4
		PRINTFORMDL Using their free hand, they reach for your %FSYN("balls")% and start %SPLIT_G("rubbing:fondling:crumpling")% them gently...
	ELSEIF ABL:(ADD_SEX_MODULE_CHARA:0):指 > 4 && ABL:(ADD_SEX_MODULE_CHARA:0):技巧 > 7 && !RAND:4
		PRINTFORMDL %CALLNAME:(ADD_SEX_MODULE_CHARA:0)% uses %HIS_HER(ADD_SEX_MODULE_CHARA:0)% free hand to %SPLIT_G("rub:fondle")% your %FSYN("balls")% at the same time...
	ELSEIF ABL:(ADD_SEX_MODULE_CHARA:1):指 && ABL:(ADD_SEX_MODULE_CHARA:1):技巧 > 7 && !RAND:4
		PRINTFORMDL %CALLNAME:(ADD_SEX_MODULE_CHARA:1)% uses %HIS_HER(ADD_SEX_MODULE_CHARA:1)% free hand to %SPLIT_G("rub:fondle")% your %FSYN("balls")% at the same time...
	ENDIF
ENDIF

@ASSISTANT_KOJO_CUSTOM_SCOM12
FOR LOCAL, 0, ADD_SEX_MODULE_ACTION_COUNT
	SIF ADD_SEX_MODULE_CHARA:LOCAL == TARGET
		CONTINUE
	CALL KOJO_MESSAGE_SEND("COMMAND_ASSISTANT",-1,ADD_SEX_MODULE_CHARA:LOCAL,-1,-1)
NEXT

@ADD_CUSTOM_SCOM_SET12
#DIM DYNAMIC H0
#DIM DYNAMIC H1

H0 = ADD_MASTER_POSE_TR(PLAYER, SET_YUBI,SET_P)
H1 = ADD_MASTER_POSE_TR(PLAYER, SET_YUBI,SET_P, 1)

SIF H0 <= 0 || H1 <= 0 ;nothing valid in the current turn
	RETURN 0

SIF H0 == H1
	RETURN 0

ADD_SEX_MODULE_ACTOR:0 = PLAYER
ADD_SEX_MODULE_CHARA:0 = H0
ADD_SEX_MODULE_CHARA:1 = H1

RETURN 1

@ADD_CUSTOM_SCOM_MODULE12
CALL ADD_SEX_MODULE_START(12)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_YUBI,,0)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_YUBI,,1)
CALL ADD_SEX_MODULE_FINISH

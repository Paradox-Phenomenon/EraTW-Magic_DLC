;Gangbang (double handjob)
@ADD_CUSTOM_SCOM19
CALL ADD_CUSTOM_SCOM_MODULE19
CALL ADD_SEX_MODULE_EXECUTE

RETURN 1

@ADD_CUSTOM_SCOM_ABLE19
#DIM DYNAMIC SUCCEED

SIF !GROUPMATCH(SELECTCOM, 71, 80, 81)
	RETURN 0

CALL ADD_CUSTOM_SCOM_MODULE19
CALL ADD_SEX_MODULE_COM_ABLE
SIF !RESULT
	RETURN 0

SIF ABL:PLAYER:技巧 < 5
	RETURN 0
SIF ABL:PLAYER:腰 < 5
	RETURN 0
SIF ABL:技巧 < 25
	RETURN 0
SIF ABL:指 < 5
	RETURN 0

RETURN 1

@ADD_CUSTOM_CAN_SCOM19(ARG)
SKIPDISP ARG
PRINTFORML Gangbang
SKIPDISP 0

CALL ADD_CUSTOM_SCOM_MODULE19
CALL ADD_SEX_MODULE_CAN_COM(ARG)
RETURN RESULT

@ADD_CUSTOM_MESSAGE_SCOM19
PRINTFORMDL %BREAKENG(@"With three of your bodies working in tandem, you insert your %TINKO_TR(MASTER)% in all of %CALLNAME:TARGET%'s holes, while %HE_SHE(TARGET)% strokes a fourth and fifth's %TINKO_TR(MASTER)% at the same time.")%

IF ABL:技巧 >= 40
	PRINTFORMDL Well-coordinated, %CALLNAME:TARGET% shakes %HIS_HER(TARGET)% hips and moves her tongue gracefully, ensuring that all of your bodies share equally in the pleasure.
ELSEIF ABL:技巧 >= 30
	PRINTFORMDL %CALLNAME:TARGET% keeps pace with you by thrusting %HIS_HER(TARGET)% hips in tandem with your movements.
ELSE
	PRINTFORMDL %CALLNAME:TARGET% has a hard time keeping up with you, and %HIS_HER(TARGET)% movements are uncoordinated.
ENDIF

@ADD_CUSTOM_SCOM_SET19
FOR LOCAL, 0, 4
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_V, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:0 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

FOR LOCAL, 0, 4
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_A, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:1 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

FOR LOCAL, 0, 4
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_M, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:2 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

FOR LOCAL, 0, 4
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_YUBI, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:3 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

FOR LOCAL, 0, 4
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_YUBI, SET_P, LOCAL)

	IF LOCAL:1 >= 0 && ADD_SEX_MODULE_ACTOR:3 != LOCAL:1
		ADD_SEX_MODULE_ACTOR:4 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

ADD_SEX_MODULE_CHARA:0 = TARGET

RETURN 1

@ADD_CUSTOM_SCOM_MODULE19
CALL ADD_SEX_MODULE_START(19)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_V,)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_A,1)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_M,2)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_YUBI,3)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_YUBI,4)
CALL ADD_SEX_MODULE_FINISH

;Triple Penetration
@ADD_CUSTOM_SCOM17
CALL ADD_CUSTOM_SCOM_MODULE17
CALL ADD_SEX_MODULE_EXECUTE

RETURN 1

@ADD_CUSTOM_SCOM_ABLE17
#DIM DYNAMIC SUCCEED

SIF !GROUPMATCH(SELECTCOM, 71, 81)
	RETURN 0

CALL ADD_CUSTOM_SCOM_MODULE17
CALL ADD_SEX_MODULE_COM_ABLE
SIF !RESULT
	RETURN 0

SIF ABL:PLAYER:技巧 < 5
	RETURN 0
SIF ABL:PLAYER:腰 < 5
	RETURN 0

RETURN 1

@ADD_CUSTOM_CAN_SCOM17(ARG)
SKIPDISP ARG
PRINTFORML Triple Penetration
SKIPDISP 0

CALL ADD_CUSTOM_SCOM_MODULE17
CALL ADD_SEX_MODULE_CAN_COM(ARG)
RETURN RESULT

@ADD_CUSTOM_MESSAGE_SCOM17
PRINTFORMDL Using your three bodies, you insert your %TINKO_TR(MASTER)% in all of %CALLNAME:TARGET%'s holes at once, stuffing %HIM_HER(TARGET)% completely.

IF ABL:技巧 >= 25
	PRINTFORMDL Eager to pleasure you further, %CALLNAME:TARGET% uses %HIS_HER(TARGET)% tongue to pleasure one of your bodies while using %HIS_HER(TARGET)% hands to caress the others.
ELSEIF ABL:技巧 >= 10
	PRINTFORMDL Shaking %HIS_HER(TARGET)% hips in tandem with your movements, %HE_SHE(TARGET)% greedily indulges in the pleasure.
ELSE
	PRINTFORMDL Going limp as %HE_SHE(TARGET)% is beset from all sides, you keeping toying with %HIS_HER(TARGET)% body...
ENDIF

@ADD_CUSTOM_SCOM_SET17
FOR LOCAL, 0, 3
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_V, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:0 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

FOR LOCAL, 0, 3
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_A, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:1 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

FOR LOCAL, 0, 3
	LOCAL:1 = ADD_CHARA_WHO_TOUCHING(TARGET, SET_M, SET_P, LOCAL)

	IF LOCAL:1 >= 0
		ADD_SEX_MODULE_ACTOR:2 = LOCAL:1
		BREAK
	ENDIF
NEXT
SIF LOCAL:1 < 0
	RETURN 0

ADD_SEX_MODULE_CHARA:0 = TARGET

RETURN 1

@ADD_CUSTOM_SCOM_MODULE17
CALL ADD_SEX_MODULE_START(17)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_V,)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_A,1)
CALL ADD_SEX_MODULE_ADD_ACTION(SET_P, SET_M,2)
CALL ADD_SEX_MODULE_FINISH

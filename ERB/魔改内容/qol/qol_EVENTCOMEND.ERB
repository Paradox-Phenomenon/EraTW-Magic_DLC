@删除立绘履历
#DIM POPSTRLINES
#LOCALSIZE 1
#LOCALSSIZE 300
;删除立绘
VARSET LOCALS
SIF FIGURE_ENDLINE == 0
	RETURN
POPSTRLINES = MIN(LINECOUNT - FIGURE_ENDLINE, 299)
FOR LOCAL, POPSTRLINES, 0, -1
	LOCALS:LOCAL = %HTML_GETPRINTEDSTR(LOCAL)%
NEXT
CLEARLINE LINECOUNT - FIGURE_STARTLINE
FOR LOCAL, POPSTRLINES, 0, -1
	HTML_PRINT LOCALS:LOCAL
NEXT

[SKIPSTART]
@DEFINE_COLUMN_(カラムプリセット名。重複不可)(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 
		RESULTS:1 = 
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = 
		RESULTS:1 = 
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 
ENDSELECT
RETURN 1

[SKIPEND]

@DEFINE_COLUMN_体力(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 1
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 体力
		RESULTS:1 = 最大体力
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = BASE,0
		RESULTS:1 = MAXBASE,0
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 22
ENDSELECT
RETURN 1


@DEFINE_COLUMN_気力(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 1
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 気力
		RESULTS:1 = 最大気力
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = BASE,1
		RESULTS:1 = MAXBASE,1
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 22
ENDSELECT
RETURN 1


@DEFINE_COLUMN_溜まってる度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 2
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 性欲
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CFLAG,340
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_処女(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11 ;TALENT:処女用専用処理
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 処女
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,0
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｃ感覚(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｃ感覚
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,0
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｖ感覚(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｖ感覚
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,1
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ａ感覚(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ａ感覚
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,2
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｂ感覚(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｂ感覚
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,3
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｍ感覚(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｍ感覚
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,4
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_親密(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 親密
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,9
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_従順(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 従順
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,10
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_欲望(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 欲望
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,11
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_技巧(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 技巧
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,12
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_奉仕精神(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 奉仕
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,13
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_露出癖(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 露出
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,14
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_指(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 指技
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,50
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_舌(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 舌技
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,51
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_胸(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 胸技
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,52
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1

@DEFINE_COLUMN_腰(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 腰技
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,53
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_膣(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 膣技
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,54
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ａ(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 4
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ａ技
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,55
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN -1
ENDSELECT
RETURN 1


@DEFINE_COLUMN_苦痛刻印(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 10
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 苦痛
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = MARK,0
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_快楽刻印(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 8
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 快楽
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = MARK,1
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_不埒刻印(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 8
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 不埒
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = MARK,2
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_反発刻印(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 9
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 反発
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = MARK,3
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_時姦刻印(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 10
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 時姦
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = MARK,5
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_好感度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 6
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 好感
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CFLAG,2
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_信頼度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 5
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 信頼
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CFLAG,4
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_キス未経験(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 初キス
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,6
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 12 ;経験表示が結構長いので
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｃ感度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｃ性質
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,101
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｖ感度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｖ性質
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,102
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ａ感度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ａ性質
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,103
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｂ感度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｂ性質
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,104
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_Ｍ感度(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = Ｍ性質
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,106
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_清掃技能(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 清掃
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,40
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_話術技能(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 話術
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,41
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_戦闘能力(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 戦闘
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,42
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_教養(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 教養
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,43
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_料理技能(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 料理
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,44
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_音楽技能(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 音楽
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ABL,45
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_釣りLv(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 釣り
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,47
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_採集Lv(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 採集
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,48
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_調合Lv(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 調合
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,49
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_舌使い(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 7
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 舌使い
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,52
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_器用な指(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 7
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 器用な指
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,51
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_自慰しやすい(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 自慰性
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,60
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_中毒しやすい(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 3
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 中毒しやすい
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,71
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_快感応答(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 快感応答
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,70
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_セフレ(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = セフレ
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,185
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_愛欲(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 愛欲
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,184
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_恋慕(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 恋慕
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,3
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_淫乱(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 淫乱
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,4
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_恋人(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 恋人
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,7
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_思慕(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 思慕
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,8
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_弾幕勝利(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 7
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 弾幕勝
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CFLAG,930
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_ムシバトル勝利(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 7
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = ムシ勝
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CFLAG,932
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_料理勝利(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 7
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 料理勝
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CFLAG,933
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_関係性一括(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 13
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 関係性
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CHARALIST_DT_RELATION
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 6
ENDSELECT
RETURN 1


@DEFINE_COLUMN_生理周期(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 15
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 生理
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = ESTRUS_CATEGORY_STR,,STR
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_発情(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 12
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 発情
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TCVAR,{GETNUM(TCVAR, "発情")}
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 4
ENDSELECT
RETURN 1


@DEFINE_COLUMN_種族概要(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1と14は特殊
		RETURN 15
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 種族
		;RESULTS:1 = 種族
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CHARA_TRIBE_STR,,STR
		;RESULTS:1 = CHARA_TRIBE_STR_SCORE
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 16
ENDSELECT
RETURN 1


@DEFINE_COLUMN_種族概要追加(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1と14は特殊
		RETURN 15
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 追加種族
		;RESULTS:1 = 種族
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = CHARA_TRIBE_STR_OPTION,,STR
		;RESULTS:1 = CHARA_TRIBE_STR_SCORE
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 16
ENDSELECT
RETURN 1


@DEFINE_COLUMN_乳サイズ(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 乳サイズ
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,{GETNUM(TALENT,"バストサイズ")}
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1


@DEFINE_COLUMN_尻サイズ(ARG_MODE = "")
#DIMS ARG_MODE
SELECTCASE ARG_MODE
	CASE DEF_COLUMN_TYPE
		;カラムが扱うデータ型をRETURNで返す
		;1だけ特殊なので注意
		RETURN 11
	CASE DEF_COLUMN_NAME
		;カラムに表示される名前を返す
		;TYPE=1なら二つ返す
		RESULTS:0 = 尻サイズ
	CASE DEF_COLUMN_VAR
		;そのカラムが参照する変数を定義。カンマより前が変数名、後ろが配列添え字
		;TYPE=1なら二つ返す
		RESULTS:0 = TALENT,{GETNUM(TALENT,"ヒップサイズ")}
	CASE DEF_COLUMN_LEN
		;カラムが使う長さを半角文字列の数で返す。-1だとNAMEの長さを測って自動設定(あまり信用しない方がいい)
		;TYPE=1は実質カラムを二つ使っているので特に長めに
		RETURN 8
ENDSELECT
RETURN 1
